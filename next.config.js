/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["avatars.githubusercontent.com", "cdn.discordapp.com", "cdn.discord.com", "cloudflare-ipfs.com"]
    }
}

module.exports = nextConfig
